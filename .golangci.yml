linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
  disable:
    - deadcode
    - varcheck

linters-settings:
  errcheck:
    check-type-assertions: true

run:
  timeout: 5m
