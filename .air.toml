root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd/server/"
  bin = "./tmp/main"
  full_bin = "./tmp/main"
  delay = 1000
  exclude_dir = ["tmp", "vendor", "bin", ".git"]
  exclude_file = [".env"]

[log]
  file = "./tmp/air.log"
